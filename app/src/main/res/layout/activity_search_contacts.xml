<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SearchContactsActivity" >

    <!-- layout to show image downloading progress -->
    <include layout="@layout/toolbar" />

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">

        <EditText
            android:id="@+id/contactSearch"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:hint="@string/searchContact_textbox_hint"
            android:singleLine="true"
            android:ems="10" />

        <Button
            android:id="@+id/searchDirectoryBtn"
            style="@style/Style.Button"
            android:layout_width="140sp"
            android:layout_marginTop="10dp"
            android:onClick="onClickDirectorySearch"
            android:text="@string/searchContact_searchdirectory_btn_lbl" />

        <ListView
            android:id="@+id/suggestionsListView"
            android:layout_width="match_parent"
            android:fadeScrollbars="false"
            android:layout_height="0dip"
            android:layout_weight="1"
            >
        </ListView>
    </LinearLayout>
</LinearLayout>